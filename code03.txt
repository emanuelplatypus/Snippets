void criarArquivo(String novoLocal) throws FileNotFoundException {
				
		File file = new File(novoLocal);
		PrintStream printStreamToFile = new PrintStream(file);
		System.setOut(printStreamToFile);
		printStreamToFile = null;
		file = null;
	}
